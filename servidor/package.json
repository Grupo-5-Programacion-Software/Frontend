{
  "name": "servidor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "json-server --watch db.json --host 10.5.225.223 --port 3000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}